FROM ubuntu:20.04

# Install premake4 and build tools
RUN apt-get update && \
    apt-get install -y premake4 build-essential && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

# Default command
CMD ["premake4", "--help"]

